export default function SettingsPage() {
  return (
    <div className="max-w-lg text-sm text-muted">
      <p>System settings, API keys, and notification channels — connect backend modules to enable.</p>
    </div>
  );
}
